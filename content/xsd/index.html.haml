---
title: XSD files
description: A list of all XSD files.
layout: base
priority: 0.1
---
%h1 #{page.title}
%ul
  - for file in Dir.entries("xsd").sort
    - if file.end_with? ".xsd"
      %li(style="margin-top: 10px;")
        %a{:href => file}
          #{file}
